# AGENTS.md

> Wrks Coding Agent(및 opencode 계열 에이전트)가 세션 시작 시 자동으로 읽는 프로젝트 지침 파일입니다. 프로젝트 루트에 위치시키세요.
> 이 파일은 VS Code의 Claude Code가 읽는 `CLAUDE.md`와 동일한 규칙을 공유하되, **원격 리눅스 환경(Wrks)에 맞게 일부 항목만 조정**한 버전입니다.
> 최종 업데이트: 2026-07-24

---

## 🔁 이 파일과 CLAUDE.md의 관계

- **`CLAUDE.md`**: VS Code + Claude Code(로컬 PC)에서 읽는 지침 파일. 로컬 환경 전용 규칙(VB6 IDE 검증, `D:\` 백업 등) 포함.
- **`AGENTS.md`(이 파일)**: Wrks Coding Agent(원격 리눅스 서버)에서 읽는 지침 파일. 두 파일은 **같은 코딩 규칙을 공유**하되, 아래 "⚙️ 환경 차이로 조정된 항목"만 다릅니다.
- 규칙 내용을 바꿀 때는 **양쪽 파일을 함께 갱신**하는 것을 권장합니다.

---

## ⚙️ 환경 차이로 조정된 항목 (CLAUDE.md 대비)

| 항목 | CLAUDE.md (로컬) | AGENTS.md (이 환경) |
|---|---|---|
| CLAUDE.md 백업 | `D:\Claude_md\`에 파일 복사 | **git commit/push로 이력 관리** (Windows 경로 없음) |
| VB6 IDE 검증 | 로컬 VB6 IDE에서 직접 확인 | **파일 편집만 가능, IDE 실행 검증은 로컬 PC에서 수행 전제** |
| 세션 로그/백업 경로 | Windows 절대경로 | **repo 내 상대경로(`docs/`)** 사용 |

---

## 👤 작업자 컨텍스트

- **소속**: 한의원 프랜차이즈 본사 전산팀
- **주요 업무**: 서버 구축/관리, 병원 차트 개발, 웹 개발, 하드웨어/소프트웨어 관리

---

## 🖥️ 개발 환경

### 서버 / OS
| 용도 | 환경 |
|---|---|
| DB 서버 | Rocky Linux 9 |
| 레거시 서버 | CentOS 7 / 7.5 |
| 윈도우 서버 | Windows Server 2012 / 2018 |

### 데이터베이스
| DB | 비고 |
|---|---|
| Oracle 11g | 메인 DB — **반드시 11g 문법 기준으로 SQL 작성** |
| MySQL | 보조 DB |
| SQL Server | Windows 서버 환경 |

### DB 툴
- Orange for Oracle
- HeidiSQL
- SQL Server Management Studio 22

---

## 💻 개발 언어 및 툴

### 병원 차트 (데스크탑)
- **언어**: Visual Basic 6.0
- **툴**: Microsoft Visual Basic IDE, Visual Studio Code
- **DB 연결**: ADODB.Connection / ADODB.Recordset
- **에러 처리**: `On Error GoTo` 방식

### 웹 개발
- **언어**: ASP (Classic), VBScript, JavaScript, HTML, CSS
- **툴**: Visual Studio Code
- **서버**: IIS
- **인코딩**: UTF-8 기준

---

## 📐 코딩 규칙

### 공통
- 주석은 **한국어**로 작성
- 설명보다 **코드 우선** 출력
- 수정 시 **변경된 부분만** 명시

### Oracle SQL (11g 기준)
- 사용 가능: `WITH`, `RANK()`, `ROW_NUMBER()`, `CONNECT BY`, `NVL`, `DECODE`
- 사용 금지: `FETCH FIRST n ROWS`, `OFFSET ... ROWS` (12c 이상 전용)
- 페이징은 `ROWNUM` 방식 사용:
  ```sql
  SELECT * FROM (
    SELECT A.*, ROWNUM AS RN FROM (...) A WHERE ROWNUM <= :end_row
  ) WHERE RN >= :start_row
  ```
- 날짜: `TO_DATE('20260101', 'YYYYMMDD')` 또는 `YYYYMMDD` 문자열

### VB6
- `Option Explicit` 항상 선언
- RecordSet 사용 후 반드시 `.Close` + `Set rs = Nothing`
- **파일 인코딩**: EUC-KR (CP949) — UTF-8 저장 금지, 한글 깨짐 주의
- **줄바꿈**: 반드시 CRLF — LF만 있으면 VB6 IDE가 `.frm`/`.bas`/`.cls`를 못 열고 `'0'을(를) 로드할 수 없습니다 / 0 행` 에러 발생

> ⚠️ **이 환경(원격 리눅스) 주의**: VB6 파일 편집은 가능하지만 **VB6 IDE로 여는 최종 검증은 로컬 PC에서만 가능**합니다. 아래 규칙들은 편집 시 반드시 지키되, "IDE에서 정상적으로 열림" 확인은 로컬에서 수행한다는 전제로 이해하세요.

#### VB6 파일(.frm/.bas/.cls) 저장 시 필수 확인사항
> 2026-07-16 실제 발생: EUC-KR↔UTF-8 왕복 변환 편집 후 `.frm` 파일이 VB6 IDE에서 "'0'을(를) 로드할 수 없습니다 / 0 행" 에러로 안 열림.
> **원인**: 반영 스크립트가 파일을 읽으면서 CRLF를 LF로 뭉갠 뒤 저장 → 파일 전체 줄바꿈이 LF로 깨짐(한글 인코딩 문제 아님).

- `.frm`/`.bas`/`.cls`를 EUC-KR↔UTF-8 왕복 변환으로 편집한 뒤에는, **반영 직후 반드시 줄바꿈이 CRLF인지 확인**한다.
  ```bash
  python -c "d=open('파일.frm','rb').read(); print('CRLF=',d.count(b'\r\n'),'LF-only=',d.count(b'\n')-d.count(b'\r\n'))"
  ```
  `LF-only`가 0이 아니면 반영이 잘못된 것 — 원본을 다시 반영하거나 LF를 CRLF로 치환 후 재확인한다.
- 위 확인 없이 "수정 완료"라고 보고하지 않는다(완료 선언 규칙과 동일한 원칙 적용).
- 이 문제가 다시 재현되면 먼저 **줄바꿈 방식**부터 의심할 것 — 인코딩(한글 깨짐) 문제로 오인해 엉뚱한 곳을 고치지 말 것.

#### FPSpread/FPUSpread 그리드 컨트롤 속성은 .frm 텍스트 직접 수정으로 반영 안 될 수 있음
> 2026-07-16 실제 발생: `Sp_Cust_list`(FPSpreadADO.fpSpread) 컨트롤의 `TextTip` 속성을 `.frm` 텍스트에 직접 추가했는데, VB6 IDE 속성창에서 확인하니 실제로는 0이었음. IDE에서 직접 바꿔야만 동작함.
> **추정 원인**: 실제 속성 상태가 `.frx` 바이너리 블록 안에 저장되어 있어, VB6이 컨트롤 로드 시 바이너리 값이 텍스트 프로퍼티를 덮어씀(재현됨).

- FPSpread/FPUSpread 그리드(`Sp_`, `vs` 접두 컨트롤)의 속성을 바꿔야 할 때는, `.frm` 텍스트를 직접 편집하지 말고 **VB6 IDE 속성창에서 직접 설정**하거나, 최소한 **설정 후 IDE 속성창에서 실제 값 재확인**한다.
- 텍스트로 먼저 넣었다면, 사용자에게 "IDE 속성창에서 실제 값 확인해달라"고 요청 — 이 확인 없이 "속성 반영 완료"라고 보고하지 않는다.
- **코드(Sub/Function) 변경은 이 문제와 무관** — `.frm` 텍스트로 코드 부분을 고치는 것은 정상 반영됨. 디자인타임 프로퍼티(Begin ~ End 블록 안 속성 값)만 해당.

#### VB6 코드 위치 안내 규칙
VB6 IDE는 줄 번호를 표시하지 않으므로, 코드 위치를 줄 번호만으로 안내하지 않는다.
수정 위치를 안내할 때는 **반드시 아래 정보를 함께 제공**한다.

**필수 제공 항목:**
1. 해당 코드가 속한 **Sub / Function / Property 이름**
2. 그 안에서의 **전후 문맥** (수정 줄 기준 위아래 2~3줄)
3. 줄 번호는 참고용으로만 표기 (`※ VS Code 기준 줄 번호, VB6 IDE에서는 확인 불가`)

**안내 형식 예시:**
```
📍 위치: Sub btnSave_Click()
   아래 코드 바로 다음 줄에 추가:

   [전후 문맥]
   If txtQty.Text = "" Then
       MsgBox "수량을 입력하세요"  ← 이 줄 아래에 추가
   ★  Call UpdateStock(...)       ← 추가할 코드
   End If
```

- 수정 위치가 여러 곳이면 **각 위치마다** 위 형식으로 안내
- "몇 번째 줄"이라는 표현 단독 사용 금지 — 반드시 메서드명 + 문맥 병기

#### VB6 수정 주석 규칙
VB6 코드 수정 시 반드시 아래 형식으로 수정 주석을 달아야 한다.

**형식:**
```vb
'YYYYMMDD 박원재 수정내용
```

**규칙:**
- 수정 범위가 한 블록에 집중된 경우 → 해당 블록 **최상단에 한 번만** 작성
- 수정 위치가 여러 곳에 분산된 경우 → **각 수정 위치마다** 개별 작성

**예시 — 블록 수정 (상단 한 번):**
```vb
'20260708 박원재 처방 저장 시 재고 차감 로직 추가
If txtQty.Text > 0 Then
    Call UpdateStock(txtItemCode.Text, txtQty.Text)
End If
```

**예시 — 분산 수정 (각각 작성):**
```vb
'20260708 박원재 NULL 체크 추가
If Not IsNull(rs("ITEM_CODE")) Then
    txtItemCode.Text = rs("ITEM_CODE")
End If

' ... 다른 코드 ...

'20260708 박원재 버튼 활성화 조건 변경
If intStatus = 1 Or intStatus = 2 Then
    btnSave.Enabled = True
End If
```

- 에러 처리 패턴:
  ```vb
  On Error GoTo ErrHandler
  ' ... 로직 ...
  Exit Sub
  ErrHandler:
      MsgBox "오류: " & Err.Description
  ```

### ASP / VBScript
- 상단 선언: `<%@ Language="VBScript" %>`
- `Response.CharSet = "UTF-8"` 설정
- DB 연결 후 반드시 `conn.Close` / `Set conn = Nothing` 처리

### Shell Script (Linux)
- `#!/bin/bash` shebang 명시
- CentOS 7 / Rocky Linux 9 구분하여 작성
- 서비스 관리: `systemctl` 사용

---

## 🔍 디버깅 규칙 (systematic-debugging)

### 핵심 원칙
> **수정 제안 전에 반드시 근본 원인(root cause)을 먼저 파악한다.**
> 증상만 고치는 것은 실패다. 추측으로 수정하지 않는다.

### 4단계 프로세스

**Phase 1 — 근본 원인 조사 (수정 전 필수)**
1. 에러 메시지 전문 읽기 (라인 번호, 에러 코드 포함)
2. 재현 가능한지 확인 — 재현 안 되면 더 많은 데이터 수집
3. 최근 변경 사항 확인 (코드, 설정, 환경)
4. 멀티 컴포넌트 시스템이면 각 경계마다 로그 추가해서 어느 지점에서 깨지는지 확인
5. 잘못된 값이 어디서 시작됐는지 호출 스택을 역으로 추적

**Phase 2 — 패턴 분석**
- 같은 코드베이스에서 정상 동작하는 유사 코드 찾기
- 정상 코드와 문제 코드의 차이점 목록화
- 의존성, 설정, 환경 가정 확인

**Phase 3 — 가설 및 테스트**
- 가설 하나를 명확히 설정: "X가 원인인 이유는 Y"
- 가설 검증을 위한 **최소한의 변경**만 적용
- 한 번에 하나씩만 수정, 결과 확인 후 다음 단계

**Phase 4 — 수정 구현**
- 근본 원인을 수정 (증상이 아닌)
- 수정 후 실제로 해결됐는지 검증
- **수정 3회 이상 실패 시 → 즉시 멈추고 구조적 문제 논의**

### 자주 발생하는 에러 패턴
| 에러 | 원인 | 확인 포인트 |
|---|---|---|
| VB6 `3709` | DB 연결 끊김 | Connection 상태 확인 |
| VB6 `800a0007` | 메모리 부족 | RecordSet 미해제 |
| Oracle `ORA-00942` | 테이블 없음/권한 없음 | 스키마, 권한 확인 |
| Oracle `ORA-01722` | 숫자 변환 실패 | 데이터 타입 불일치 |
| Oracle `ORA-01000` | 커서 초과 | RecordSet 미닫힘 |
| ASP `500` | 서버 에러 | IIS 로그 확인 |

### 하지 말아야 할 것
- "일단 X 바꿔보자" 식의 추측 수정
- 원인 모르는 상태에서 여러 곳 동시 수정
- 에러 메시지 대충 읽기
- 수정 3회 이상 실패 후 계속 시도

---

## 📋 새 기능 개발 프로세스 (brainstorming → writing-plans)

### 구현 전 필수 단계
> 코드 한 줄 작성 전에 아래 단계를 완료한다.

1. **현재 프로젝트 구조 파악** — 기존 파일, 패턴, 최근 변경 사항 확인
2. **요구사항 명확화** — 목적, 제약 조건, 완료 기준 질문 (한 번에 하나씩)
3. **2~3가지 접근 방식 제안** — 트레이드오프와 추천 방식 포함
4. **설계 확정 후 구현 계획 작성**

### 구현 계획 문서 형식
계획 문서는 `docs/plans/YYYY-MM-DD-기능명.md`에 저장:

```markdown
# [기능명] 구현 계획

**목표:** 한 문장으로
**아키텍처:** 2~3문장으로
**기술 스택:** 사용 언어/라이브러리

---

### Task 1: [컴포넌트명]

**파일:**
- 생성: `경로/파일명`
- 수정: `경로/파일명`

- [ ] Step 1: ...
- [ ] Step 2: ...
- [ ] Step 3: 커밋
```

### 계획 작성 원칙
- 각 단계는 2~5분 내 완료 가능한 단위로
- 실제 코드를 단계마다 포함 (TBD, TODO 금지)
- 정확한 파일 경로 명시
- 계획 작성 후 요구사항과 대조해서 누락 여부 확인

---

## ✅ 완료 선언 규칙 (verification-before-completion)

### 핵심 원칙
> **검증 없는 완료 선언은 거짓말이다.**
> 반드시 실행 결과를 확인한 후에만 "완료"라고 한다.

### 완료 선언 전 체크게이트
```
1. 완료를 증명할 명령어/방법이 무엇인가?
2. 실제로 실행했는가?
3. 출력 결과를 직접 읽었는가?
4. 결과가 완료를 증명하는가?
   → YES: 근거와 함께 완료 선언
   → NO: 실제 상태 보고
```

### 하지 말아야 할 것
- "동작할 것 같습니다" / "아마 될 거예요" 식의 표현
- 실행 안 하고 "완료됐습니다" 선언
- 부분 확인만 하고 전체 완료 주장
- 에이전트 리포트만 믿고 직접 확인 생략

---

## 📋 수정 작업 완료 요약 규칙

### 핵심 원칙
> 여러 번 승인을 거쳐 수정 작업이 완료되면,
> **마지막에 반드시 전체 수정 내역을 아래 형식으로 한 번에 정리해서 출력한다.**

### 출력 형식
```
---
## 📋 수정 작업 완료 요약

### 수정 파일
- `파일명.bas` — 수정 내용 한 줄 요약
- `파일명.frm` — 수정 내용 한 줄 요약

### 변경 사항
| 위치 | 수정 전 | 수정 후 |
|---|---|---|
| 함수명 또는 라인 | 기존 내용 요약 | 변경 내용 요약 |

### 수정 이유
- 간단한 설명

### 추가 확인 필요 사항
- 테스트 필요 항목 또는 없으면 "없음"
---
```

### 규칙
- 중간 승인 횟수와 무관하게 **작업 종료 시 항상 출력**
- 수정 파일이 1개여도 동일하게 출력
- "추가 확인 필요 사항"이 없으면 "없음"으로 명시
- 요약은 **한국어**로 작성

---

## 🗂️ 자주 쓰는 작업 패턴

### Excel/CSV → Oracle INSERT SQL
- 날짜 컬럼: `TO_DATE('값', 'YYYYMMDD')`
- 빈 값: `NULL` 처리
- 시퀀스: `시퀀스명.NEXTVAL`

### 배포 체크리스트
- [ ] DB 연결 문자열 운영 환경으로 변경
- [ ] 에러 처리 로직 확인
- [ ] 로그 파일 경로 확인
- [ ] 인코딩 UTF-8 확인
- [ ] 테스트 데이터 정리

---

## 📁 세션 로그 저장 규칙

### 핵심 원칙
> 각 대화 세션의 주요 내용을 파일로 저장하여 새 세션에서도 이전 작업 맥락을 파악할 수 있도록 한다.

### 저장 위치
```
docs/sessions/YYYY-MM-DD_세션주제.md
```
예: `docs/sessions/2026-07-21_보험청구화면_코드스캔.md`

### 저장 시점
- 사용자가 "세션 저장해줘" / "로그 남겨줘" 요청 시
- 장시간 분석·스캔 작업이 완료됐을 때
- 다음 세션에서 이어받아야 할 작업이 있을 때

### 저장 내용 형식
```markdown
# [세션 주제]
> 날짜: YYYY-MM-DD | 세션: [식별자]

## 작업 목표
- 이 세션에서 하려 했던 것

## 분석/확인한 내용
- 파일명, 함수명, 핵심 발견사항 목록

## 결정 사항
- 채택한 방향, 기각한 이유

## 다음 세션에서 할 일
- [ ] 구체적인 다음 작업 항목

## 참고: 주요 파일·함수 위치
| 항목 | 파일 | 위치 |
|---|---|---|
| 함수명 | 파일명.bas | 줄번호 |
```

### 규칙
- 파일명에 날짜 필수 포함 (YYYY-MM-DD)
- 한국어로 작성
- 코드 변경 내역은 이 파일이 아닌 수정 작업 완료 요약 형식으로 별도 작성
- `docs/sessions/` 폴더가 없으면 먼저 생성

---

## 💾 지침 파일 백업/버전 관리 규칙 (이 환경 조정)

### 핵심 원칙
> 로컬(`CLAUDE.md`)의 `D:\Claude_md\` 폴더 백업 규칙은 **원격 리눅스 환경에는 적용되지 않는다.**
> 이 환경에서는 **git 커밋/푸시로 지침 파일의 이력을 관리**한다.

### 동작 방식 (이 환경)
1. `AGENTS.md`(또는 `CLAUDE.md`) 내용에 변경이 생기면
2. 변경을 **git commit**하여 이력을 남긴다 (커밋 메시지에 변경 요지 명시)
3. `origin`으로 **push**하여 원격에 반영
4. 별도의 Windows 경로(`D:\...`) 백업은 이 환경에서 수행하지 않는다 — 로컬 PC의 Claude Code가 담당

### 규칙
- 지침 파일 변경은 **의미 있는 단위로 커밋** (규칙 추가/수정/삭제 구분)
- `AGENTS.md`와 `CLAUDE.md`의 공통 규칙을 바꿀 때는 **두 파일을 함께 갱신**하는 것을 권장
- 버전 이력은 git 히스토리로 대체 (별도 날짜별 파일 불필요)

---

> ✏️ 환경 변경 시 이 파일을 업데이트하세요. (공통 규칙 변경 시 `CLAUDE.md`도 함께 갱신 권장)
